# hadwiger-nelson-lab

Computational research workspace for the Hadwiger–Nelson problem, currently focused on five-color forcing structures handed off from [`HeliCorgi/five-color-forcing-anatomy`](https://github.com/HeliCorgi/five-color-forcing-anatomy).

> This repository does **not** claim a solution of the Hadwiger–Nelson problem. Results here are finite computational intermediate statements with explicitly stated scope.

## Result-recording policy

Whenever a search produces a lemma-like or theorem-like mathematical statement, record it together with:

- the exact statement and scope;
- why it follows mathematically from the computed object;
- the generating code and compact result/certificate path;
- solver/checker identity and whether an independent implementation rechecked it;
- an explicit warning when the statement is conditional or does not imply a new Hadwiger–Nelson bound.

Large reproducible SAT proof traces should normally remain GitHub Actions artifacts rather than Git blobs. The repository retains regeneration code, compact metadata, hashes/artifact IDs, and the human-readable mathematical claim.

Potentially interesting structure should also be preserved even when it is not yet a theorem or a new lower bound. See [`STRUCTURAL_NOTES.md`](STRUCTURAL_NOTES.md) for human-readable notes separating verified finite statements, exact computational observations, empirical patterns, and conjectural interpretations. The working rule is to prefer an explanation a human can inspect over an isolated solver verdict whenever such a compression can be found.

## Verified mathematical/computational result log

All statements below use upstream commit

`d1e80998bda337d9fa721f2e96d203ae54e97fc8`.

### Q1. Critical quotient for the P2 forcing relation

Let `Q = H*/C88` be the quotient obtained by contracting the 88 prescribed equality conditions, and let `p,q` be the quotient vertices containing original vertices 217 and 490.

The checked quotient has **305 vertices and 1599 edges**. `Q` is 5-colorable, while

`G = Q + pq`

is not 5-colorable. Deleting any one vertex from `G` makes it 5-colorable, so this added-edge obstruction is vertex-critical at the checked quotient level. `G` has no articulation point and no 2-vertex cut, and the `p`-to-`q` vertex connectivity in `Q` is **6**.

Evidence: [`results/quotient-critical/analysis.json`](results/quotient-critical/analysis.json), generated by [`tools/quotient_critical.py`](tools/quotient_critical.py).

### Q2. Local common-neighborhood forcing lemma

For any graph in a proper 5-coloring, if the common-neighborhood graph of vertices `u,v` is not 3-colorable, then `c(u)=c(v)`: if `u,v` used distinct colors, their common neighbors would have only the other three colors available.

In the checked quotient, port `p` and quotient node 22 (original vertex 36) have a common neighborhood exactly equal to `K4`, so they are forced equal by this local rule. This does **not** by itself connect `p` to `q`.

Evidence: [`results/local-equality-chain/analysis.json`](results/local-equality-chain/analysis.json), generated by [`tools/local_equality_chain.py`](tools/local_equality_chain.py).

### Q3. Six-vertex color-symmetric separator interface

A minimum `p`-`q` vertex separator in `Q` is

`S = {0, 6, 8, 9, 10, 266}`.

Deleting `S` separates a 261-vertex component containing `p` from a 38-vertex component containing `q`.

There are 202 canonical equality partitions of six labeled boundary vertices using at most five colors. The CaDiCaL discovery computation found only one boundary state extendable to both sides. A separate Glucose4 implementation independently enumerated all 202 states on both sides and verified:

- left / `p` side extendable states: 2;
- right / `q` side extendable states: 81;
- intersection: exactly one state, `012203`.

The restricted-growth state `012203` has blocks

`{q0,q10} | {q6} | {q8,q9} | {q266}`.

For that unique common state, both side solvers force their port to the same singleton boundary color, the block containing `q6`. Therefore every proper 5-coloring of `Q` satisfies

`c(217) = c(490)`.

This is a **color-symmetric six-vertex interface proof of the conditional P2 forcing statement**. It is not an unconditional unit-distance forcing gadget and does not prove `χ(R²) >= 6`.

Discovery: [`results/separator-interface/analysis.json`](results/separator-interface/analysis.json). Independent verification: [`results/separator-verify/analysis.json`](results/separator-verify/analysis.json).

### P1. Independently checked UNSAT proof for the P2 contradiction

The SAT contradiction is normalized as

`COMMON ∧ A-only ∧ B-only`,

where `COMMON` is the shared 5-coloring encoding of `H*`, `A-only` contains the C88 equalities, and `B-only` contains `c(217) != c(490)`.

The normalized CNF has 1965 Boolean variables and 13,203 clauses: 12,318 common, 880 A-only, and 5 B-only.

Glucose4 produced an UNSAT DRUP proof. The proof was independently processed with pinned `drat-trim` commit `2e3b2dc0ecf938addbd779d42877b6ed69d9a985`, converted to LRAT, and accepted by the independent `lrat-check` checker.

The trimmed LRAT dependency cone contains 11,018 initial clauses and 215,041 learned clauses. All 880 A-only clauses, all 5 B-only clauses, and all 393 graph vertices remain represented, so ordinary proof trimming does not expose a small local cause.

Compact evidence: [`results/proof-lrat/PARTITION.json`](results/proof-lrat/PARTITION.json) and [`results/proof-lrat/LRAT_CONE.json`](results/proof-lrat/LRAT_CONE.json). Regeneration/analysis: [`tools/lrat_export.py`](tools/lrat_export.py).

The full proof files remain in Actions artifact `10185491584` from run `34564305431`; uploaded ZIP SHA-256: `fc062d366b78700baf008ef18b8d424a30400717691c8be42f2ff153371c423f`.

### P2. Proof-guided interpolation probe

Learned clauses were mined for 4-, 5-, and 6-vertex supports. For each size, 180 candidate supports were projected to canonical equality partitions on both sides, for 540 exact checks total.

No sampled support separated A from B. The minimum A/B state overlap was 1 for each support size. This is a useful search observation, **not** a completeness theorem.

Evidence: [`results/proof-interpolant-deep/DEEP_SUMMARY.json`](results/proof-interpolant-deep/DEEP_SUMMARY.json), generated by [`tools/proof_interpolant_deep.py`](tools/proof_interpolant_deep.py).

## Current lane — port-free equality interfaces

Define

- `A = H* ∧ C88`;
- `B = H* ∧ (c(217) != c(490))`.

For a set `S` of non-port vertices, the observable is the equality partition induced on `S` by a proper 5-coloring. Searching exactly four vertices is complete for support `<= 4`: a separating equality interface on fewer vertices remains separating after arbitrary non-port vertices are added.

For every validated A/B fooling pair `(alpha,beta)`, form a difference graph on the 391 non-port vertices, with edge `{u,v}` exactly when

`[alpha(u)=alpha(v)] != [beta(u)=beta(v)]`.

A four-set can separate A from B only if its six internal pair-atoms hit every accumulated difference graph. This turns the support-4 master into an exact finite hitting problem.

### Latest exact computational observation — support-4 master exhausted

GitHub Actions run [`34683854626`](https://github.com/HeliCorgi/hadwiger-nelson-lab/actions/runs/34683854626) completed successfully on 2026-09-12 using the monotone-resumable exact four-set master.

Final durable state:

- non-port universe: **391** vertices / **76,245** pair-atoms;
- validated upstream seed fooling pairs: **872**;
- lab-generated fooling pairs: **6139**;
- total validated fooling pairs before cut deduplication: **7011**;
- unique difference-graph cuts: **7008**;
- exact duplicate cuts: **3**;
- fixed pivot cut: `checkpoint:5191`, with **23,440** pivot edges;
- completed pivot roots: **23,440 / 23,440**;
- terminal status: `EXACT-MASTER-EXHAUSTED-REQUIRES-INDEPENDENT-VERIFY`.

The exact-master output states:

> No four-set of the 391 non-port vertices hits every validated unique difference-graph cut in this library.

If independently reconstructed and verified, this implies that **the pinned A/B system has no port-free equality semantic interface supported on at most four vertices**.

This is currently an **exact computational observation, not yet an independently verified finite theorem**. It is conditional on the pinned P2/C88 system, concerns only this equality-observable language, and does not imply a new Hadwiger–Nelson lower bound.

Compact evidence:

- [`results/exact-cegis-loop/MASTER_FINAL.json`](results/exact-cegis-loop/MASTER_FINAL.json)
- [`results/exact-cegis-loop/STATUS.md`](results/exact-cegis-loop/STATUS.md)
- [`results/exact-cegis-loop/master-current/STATE.json`](results/exact-cegis-loop/master-current/STATE.json)
- [`results/exact-cegis-loop/master-current/ROOT_ORDER.json`](results/exact-cegis-loop/master-current/ROOT_ORDER.json)

Run artifact:

- artifact id: `10298170797`;
- ZIP SHA-256: `33be9aa4456a8fb293811c39b5a4bf58574d9a34c9f8bbc812b5091a7fb96d29`;
- final cut-library SHA-256: `a58cb579f1fa725107ef10a776b290c089a8ff0cfc12dc4d75fa1fe82ba5ccfa`;
- fixed root-order SHA-256: `f72cdbf49f6a06929ebae1efe58b795b466c1ef4bd5bfa2f923ee6b3029532a7`.

### What the monotone-resume run actually established operationally

The previous exact loop restarted from root 0 after each new SAT fooling-pair cut. The v2 master fixes the pivot/root order and verifies that the new ordered unique-cut library is a prefix extension of the old one. Because added cuts only strengthen the hitting constraints, a root branch already exhaustively rejected under the old cut set remains rejected after new cuts are appended; only the candidate root must be retried.

Run `34683854626` exercised this mechanism in production:

- it upgraded the legacy state and resumed at root rank **4833**, rather than 0;
- candidate support `{318,394,399,444}` appeared at root rank **10484**; both CaDiCaL195 and Glucose4 found a semantic fooling pair, producing lab pair 6138, and the master resumed from root **10484** with `resume-monotone-prefix-v2`;
- candidate support `{4,301,402,464}` appeared at root rank **21442**; again both semantic solvers returned SAT, producing lab pair 6139, and the master resumed from root **21442**;
- the final strengthened library was then exhausted through root **23440**.

Thus the intended no-restart monotonicity optimization was not merely self-tested; it was exercised twice across real cut additions in the terminal run.

## Historical CEGIS note — Run 4 counter correction

Run [`34562068005`](https://github.com/HeliCorgi/hadwiger-nelson-lab/actions/runs/34562068005) ended with 5910 lab pairs and 6782 total represented cuts. It added 2705 sound cuts relative to Run 2, with 2705 exact-oracle calls and zero oracle UNKNOWNs.

The checkpoint counter `fast_candidates` is **2702**, not 2705. Therefore it is not justified to say that all 2705 oracle calls came from the fast layer without tracing the three-call difference. Earlier README/HANDOFF wording that equated those counters was incorrect and has been removed.

Run-4 artifact id `10187878072`, digest `sha256:f32e432c7f4cc8b30ce9ddbc449e9e8b23d863ad0d83772c745ed73cb1be8bcd`.

## Next proof obligation

Do **not** promote the support-4 exhaustion to a theorem solely from the producing implementation.

The next high-value task is an independent reconstruction of the same finite statement using a materially different exhaustive decomposition or a standalone proof-tree/certificate checker. Good options include:

- an independent triple-to-fourth enumeration over the reconstructed cut library;
- a proof tree whose internal nodes select an uncovered cut and whose branches cover every compatible cut edge, with leaves carrying an explicit blocking cut;
- a separate generic SAT master as a secondary backstop, although that is less structurally independent.

Only after an independent checker agrees should the README move the support-4 nonexistence statement into the verified result log.

Even after such verification, the conclusion would be limited: this particular **port-free equality observable language** would require support at least five. It would not rule out the already verified six-vertex quotient separator, other semantic observables, or a direct geometric forcing gadget.

The eventual Hadwiger–Nelson objective remains geometric: obtain an unconditional forced-mono pair at Euclidean distance at least `1/2`, or an equivalent finite 6-chromatic unit-distance construction.

See [`HANDOFF.md`](HANDOFF.md) for the operational continuation plan and certificate policy, and [`RESEARCH.md`](RESEARCH.md) for the mathematical setup.