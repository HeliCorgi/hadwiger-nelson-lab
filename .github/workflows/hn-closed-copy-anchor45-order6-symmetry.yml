name: hn-closed-copy-anchor45-order6-symmetry

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/hn-closed-copy-anchor45-order6-symmetry.yml'
  workflow_dispatch:

permissions:
  contents: read

env:
  UPSTREAM_SHA: d1e80998bda337d9fa721f2e96d203ae54e97fc8
  G510_SHA256: 0248fe0c7fd2df06d818aee0c188a7b70362d5990c1ca39271fc057b7b898346
  OUT_DIR: results/unconditional-2026-09-14/closed-copy-anchor45-order6-symmetry

jobs:
  symmetry-sat:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v7
        with:
          ref: main
      - uses: actions/checkout@v7
        with:
          repository: HeliCorgi/five-color-forcing-anatomy
          ref: ${{ env.UPSTREAM_SHA }}
          path: upstream
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
          cache: pip
      - name: Install dependencies
        run: |
          python -m pip install -r requirements.txt
          python -m pip install scipy==1.14.1
      - name: Verify pinned G510
        run: echo "$G510_SHA256  upstream/handoff/data/g510_k2.json" | sha256sum -c -
      - name: Build closed order-6 candidate
        run: |
          PYTHONPATH=tools python tools/hn_closed_copy_direct.py \
            --graph upstream/handoff/data/g510_k2.json --out-dir "$OUT_DIR" \
            --order 6 --anchor 45,173
      - name: Exact all-pairs completion
        run: |
          g++ -O3 -std=c++17 tools/hn_exact_completion.cpp -o /tmp/hn_exact_completion
          python tools/hn_exact_complete_io.py prepare --graph "$OUT_DIR/SELECTED_GRAPH.json" --out "$OUT_DIR/POINTS.txt"
          /tmp/hn_exact_completion < "$OUT_DIR/POINTS.txt" > "$OUT_DIR/EXACT_EDGES.txt" 2> "$OUT_DIR/EXACT_COMPLETION.log"
          python tools/hn_exact_complete_io.py finish --graph "$OUT_DIR/SELECTED_GRAPH.json" --edges "$OUT_DIR/EXACT_EDGES.txt" --out-dir "$OUT_DIR"
      - name: Sound triangle-symmetry 5-color probe
        run: |
          PYTHONPATH=tools python tools/hn_symmetry_sat_probe.py \
            --graph "$OUT_DIR/COMPLETED_GRAPH.json" --out-dir "$OUT_DIR/symmetry" \
            --conflicts 2000000 --attempts 6 --seed 20260914
      - name: Summarize
        if: always()
        run: |
          cp "$OUT_DIR/symmetry/SYMMETRY_SAT.json" "$OUT_DIR/SYMMETRY_SAT.json" 2>/dev/null || true
          echo '# Closed copy anchor45 order6 symmetry probe — 2026-09-14' > "$OUT_DIR/SUMMARY.md"
          echo >> "$OUT_DIR/SUMMARY.md"
          cat "$OUT_DIR/SCREEN.json" >> "$OUT_DIR/SUMMARY.md" 2>/dev/null || true
          cat "$OUT_DIR/GEOMETRY.json" >> "$OUT_DIR/SUMMARY.md" 2>/dev/null || true
          cat "$OUT_DIR/SYMMETRY_SAT.json" >> "$OUT_DIR/SUMMARY.md" 2>/dev/null || true
          cat "$OUT_DIR/SUMMARY.md"
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: hn-closed-copy-anchor45-order6-symmetry-${{ github.run_id }}
          path: ${{ env.OUT_DIR }}
          if-no-files-found: error
          retention-days: 30
